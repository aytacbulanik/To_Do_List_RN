import React from 'react';
import {SafeAreaView,View,Text} from 'react-native';

const todoApp = () => {
  return(
    <SafeAreaView>
      <View>
        <Text> deneme </Text>
      </View>
    </SafeAreaView>
  )
};

export default todoApp;